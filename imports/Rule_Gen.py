def Rule_Gen(supMap, cand_set, minconf):
    print("...rule generation...")

    if n-1 < 0:
        return

    right = list(cand_set)
    left = list(cand_set)
    conf = 0.0
    n = len(cand_set)

    #find all itemsets in supMap that are n-1
    listN = 
    
